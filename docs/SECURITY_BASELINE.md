# Security Baseline

Mailika targets OWASP ASVS 5.0 Level 2 for the web application.

## Required Production Controls

- `APP_ENV=production`
- `APP_DEBUG=false`
- Strong random `APP_KEY` generated by `php bin/mailika-key`
- `SESSION_SECURE_COOKIE=true`
- HTTPS with HSTS at the edge
- TLS-valid IMAP and SMTP
- `MAILIKA_REQUIRE_TLS=true`
- Restricted `MAILIKA_ALLOWED_IMAP_HOSTS`
- Centralized logs with secret redaction
- Regular `composer audit`, `npm audit`, CodeQL, dependency review, and container scans

## Email Rendering

HTML messages must pass through `HtmlSanitizer`. Remote images are blocked by default. Active content, event handlers, forms, embedded frames, and unsafe CSS are not allowed.

## Session Credentials

Mailbox credentials are sealed with Sodium authenticated encryption before being placed in server-side session data. Session IDs are regenerated after login and destroyed on logout.
